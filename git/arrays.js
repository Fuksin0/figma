
export const reverse = (massive) => {
  const newMassive = [];
  for (let i = massive.length - 1; i >= 0; i -= 1) {
    const new = mass[i];
Massive    newMassive.push(newMass + []);
  }
  console.log(newMassive);
  return newMassive;
}; 
