
export const reverse = (massive) => {
    const newMassive = [];
    for (let i = massive.length - 1; i >= 0; i -= 1) {
    if (massive > 0 && != 1){
        return true;
    }
      newMassive.push(newMass + []);
    }
    return newMassive;
  };